import Vue from 'vue'
import { Button, NavBar, Field, CellGroup, Cell,
  Toast, CountDown, Tabbar, TabbarItem, Image,
  Grid,
  GridItem,
  Icon
} from 'vant'
Vue.use(Button) // 注册
  .use(NavBar)
  .use(Field)
  .use(CellGroup)
  .use(Toast)
  .use(CountDown)
  .use(Tabbar)
  .use(TabbarItem)
  .use(Image)
  .use(Grid)
  .use(GridItem)
  .use(Icon)
  .use(Cell)
